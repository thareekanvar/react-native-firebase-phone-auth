import { captureRef } from 'react-native-view-shot';
export default async function takeSnapshotAsync(node, options) {
    return captureRef(node, options);
}
//# sourceMappingURL=takeSnapshotAsync.js.map